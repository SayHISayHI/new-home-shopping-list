# 新居采购清单 (New Home Shopping List) - 开发文档与产品原型

## 一、 项目背景与目标
**背景：** 首次租房或搬新家时，需要采购的物品繁多且琐碎，容易遗漏或让人感到焦头烂额。
**目标：** 开发一款简单、美观且实用的采购清单 Web 应用，帮助用户（如女友及其他有类似需求的用户）有条理地整理和追踪购物进度。

## 二、 产品原型设计 (UI/UX)
**整体风格：**
*   **主题色彩：** 温暖、复古的淡黄色纸质风格 (Pale yellow paper style)，类似于羊皮纸或手账本，给人温馨、安心的感觉。
*   **字体排版：** 全局使用优雅的衬线体 (Serif fonts)，如 `Noto Serif SC` 或 `Songti`，增强文字的阅读感和纸质感。
*   **交互动画：** 轻量级的划线动画、星级评分的交互反馈。

**核心界面元素：**
1.  **顶部导航/Header：** 
    *   应用标题：“新居采购清单”。
    *   操作按钮区：导入 (Import JSON)、导出 (Export JSON / 导出为图片)。
2.  **分类标签栏 (Tabs)：** 
    *   横向滑动的分类切换，当前选中的分类高亮显示。
3.  **清单列表区 (List Area)：** 
    *   每个商品项显示为一行。
    *   **左侧：** 复选框 (Checkbox)。勾选后，整个商品项文字变灰并带有划线效果 (Strikethrough)。
    *   **中间：** 商品名称输入框/展示。
    *   **右侧：** 重要程度评分组件（一星到五星，支持半星）。
    *   **操作：** 每行末尾可删除，列表底部有“+ 添加新物品”按钮。

## 三、 功能规格说明书
1.  **科学分类 (Categories)：**
    参考主流电商平台的家居频道结构，默认提供以下分类，并允许用户自定义增删：
    *   **卧室用品 (Bedroom)：** 床垫、四件套、枕头、衣架等。
    *   **卫浴洗护 (Bathroom)：** 毛巾、牙刷杯、沐浴露、马桶刷等。
    *   **餐厨用品 (Kitchen)：** 锅具、餐具、刀具、保鲜盒等。
    *   **清洁收纳 (Cleaning)：** 扫把、拖把、垃圾桶、收纳箱、清洁剂等。
    *   **日用百货 (Daily Essentials)：** 纸巾、插排、剪刀、胶带等。
    *   **家用电器 (Appliances)：** 吹风机、热水壶、微波炉等。
    *   **软装装饰 (Decor)：** 窗帘、地毯、绿植等。
2.  **物品管理 (Item Management)：**
    *   **重要度排名：** 支持 1-5 星评分（支持半星），用于区分“急需购买”和“可稍后购买”的物品。
    *   **状态切换：** 未购买 / 已购买。已购买项自动添加删除线。
3.  **本地持久化存储 (Local Storage)：**
    *   使用浏览器的 `localStorage` 进行数据存储，确保刷新或重新打开页面时不丢失数据。
    *   数据结构设计为包含分类及子项的 JSON 对象。
4.  **数据导入导出 (Import/Export)：**
    *   **导出为 JSON：** 允许用户将整个清单备份到本地电脑。
    *   **导入 JSON：** 方便将清单分享给他人或从备份恢复。
    *   **导出为图片：** 使用 `html2canvas` 库，将清单渲染为图片，方便用户保存到手机相册或在微信等社交软件中分享。

## 四、 技术架构与选型
*   **前端框架：** 原生 HTML/JS 或轻量级框架 (如 Vue 3 / React) 均可。考虑到开发效率和组件化，推荐使用 **Vue 3 (Composition API)** + Vite 构建。若要求最简依赖，也可以使用 Vanilla JS。
*   **CSS 框架：** **Tailwind CSS**，用于快速构建纸质风格和响应式布局。
*   **图标库：** Heroicons 或 FontAwesome (提供星标和功能图标)。
*   **辅助库：**
    *   `html2canvas` 或 `dom-to-image`：用于实现导出为图片功能。
    *   `file-saver`：用于方便地触发文件下载。

## 五、 数据结构设计 (Data Model)
存储在 `localStorage` 中的核心数据结构：
```json
{
  "categories": [
    {
      "id": "c1",
      "name": "卧室用品",
      "items": [
        {
          "id": "i1",
          "name": "纯棉四件套",
          "importance": 5.0,
          "completed": false
        },
        {
          "id": "i2",
          "name": "床头灯",
          "importance": 3.5,
          "completed": true
        }
      ]
    },
    // ... 其他分类
  ]
}
```

## 六、 开发步骤计划
1.  **Step 1: 环境搭建** - 初始化 Vite 项目，配置 Tailwind CSS。
2.  **Step 2: 界面静态切图** - 根据原型设计，编写整体布局、纸质背景样式、衬线字体配置，以及清单项的静态展示（包含打分组件）。
3.  **Step 3: 状态管理与交互** - 实现物品的添加、删除、勾选划线功能，实现星级评分交互。
4.  **Step 4: 本地存储接入** - 监听数据变化并实时保存到 `localStorage`，并在初始化时读取。
5.  **Step 5: 导入导出功能** - 实现 JSON 文件的上传解析与下载生成；引入图片生成库实现分享截图功能。
6.  **Step 6: 测试与优化** - 跨设备显示测试（重点是移动端适配），美化 UI 细节。